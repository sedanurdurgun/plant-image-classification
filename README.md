# plant-image-classification
Plant image classification project using transfer learning with ResNet50. The model is trained on plant images and fine-tuned to learn plant-specific features for multi-class classification.
